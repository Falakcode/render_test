# Production Dependencies for Render.com Worker
# Combines: Tick Streaming + Economic Calendar + Financial News Scraper
# WebSocket client for TwelveData streaming
websockets>=13.0
# HTTP client (explicit version for compatibility)
httpx>=0.27.0
# HTTP requests for RSS feeds and web scraping
requests==2.31.0
# HTML parsing for Trading Economics calendar
beautifulsoup4==4.12.2
# Supabase database client (Python 3.13 compatible)
supabase>=2.9.0
# DeepSeek AI for news classification (OpenAI-compatible API)
openai>=1.0.0
# RSS feed parsing
feedparser==6.0.11
# Environment variables (for local testing)
python-dotenv==1.0.0
# Timezone handling with DST support
pytz>=2024.1
